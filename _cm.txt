Add deploy config: Render blueprint for backend + frontend

- render.yaml now defines both services: delta-connector-api (FastAPI,
  uvicorn on $PORT) and delta-connector-web (TanStack Start SSR, Node).
- frontend/vite.config.ts: force nitro on with the node-server preset so
  the build emits a standalone server (dist/server/index.mjs) listening
  on $PORT. Verified: builds, boots, serves 200.
- frontend/.env.example documents VITE_API_BASE (build-time).
- frontend/.node-version pins Node 22.17.0.
- DEPLOY.md: Blueprint deploy steps + wiring ALLOWED_ORIGINS / VITE_API_BASE.
- Includes the My Network graph white-background tweak.
